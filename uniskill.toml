# Project-level config — installs dev skill into the built-in pi harness.
# We use our own CLI to wire our own workflow: uniskill-dev → ~/.agents/skills/

[[bundles]]
source = "./uniskill-bundle"
harnesses = ["pi"]
