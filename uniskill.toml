# Project-level uniskill config — installs dev skills locally.
# Demonstrates self-hosting: we use our own tool to wire our own workflow.

[harnesses.dev]
label = "Dev Harness"
scope = "project"
pattern = ".dev-skills/{name}"

[[bundles]]
source = "./uniskill-bundle"
harnesses = ["dev"]
