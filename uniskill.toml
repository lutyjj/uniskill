# Project-level config — installs dev skill into a local harness scoped to this repo.
# The skill lives at .agents/skills/uniskill-dev inside the project, not globally.

[harnesses.agents]
scope = "project"
pattern = ".agents/skills/{name}"

[[bundles]]
source = "./uniskill-bundle"
harnesses = ["agents"]
